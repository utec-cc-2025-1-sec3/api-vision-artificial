org: mejiafernandezo
service: api-vision-artificial

provider:
  name: aws
  runtime: python3.13
  memorySize: 1024
  timeout: 20
  iam:
    role: arn:aws:iam::871132477804:role/LabRole

functions:
  listar:
    handler: DetectarEtiquetas.lambda_handler
    events:
      - http:
          path: /images/etiquetas
          method: post
          cors: true
          integration: lambda
